{
  "name": "todolist",
  "version": "1.0.0",
  "private": true,
  "description": "A simple Node.js + Express + MongoDB to-do list application.",
  "main": "bin/www",
  "scripts": {
    "start": "node ./bin/www",
    "start:prod": "NODE_ENV=production node ./bin/www",
    "prestart": "npm install --omit=dev || true",
    "build": "echo \"Application is built (no compilation needed for this project)\"",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "cookie-parser": "~1.4.6",
    "date-fns": "^4.1.0",
    "debug": "~4.3.7",
    "express": "^4.21.0",
    "http-errors": "~2.0.0",
    "mongoose": "^8.6.3",
    "morgan": "~1.10.0",
    "pug": "^3.0.3"
  },
  "engines": {
    "node": "20.x"
  }
}

